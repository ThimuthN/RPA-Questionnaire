{
  "buildCommand": "if [ \"$VERCEL_ENV\" = \"production\" ]; then if [ -n \"$SKIP_MIGRATIONS\" ]; then npx prisma db push --skip-generate --accept-data-loss; else npm run prisma:migrate:deploy; fi && npm run addons:bootstrap; fi && npm run build"
}
